html_content = """
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Happy Birthday Surjo!</title>
    <style>
        body {
            background: linear-gradient(to right, #ffecd2, #fcb69f);
            font-family: 'Comic Sans MS', cursive, sans-serif;
            text-align: center;
            padding: 50px;
            animation: fadeIn 2s ease-in;
        }

        h1 {
            font-size: 3em;
            color: #ff4b2b;
        }

        p {
            font-size: 1.5em;
            color: #444;
        }

        .quote {
            margin-top: 40px;
            font-style: italic;
            color: #555;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .balloon {
            width: 50px;
            height: 70px;
            background: red;
            border-radius: 50% 50% 50% 50% / 60% 60% 40% 40%;
            position: absolute;
            animation: float 6s ease-in-out infinite;
        }

        @keyframes float {
            0% { transform: translateY(0); }
            50% { transform: translateY(-100px); }
            100% { transform: translateY(0); }
        }
    </style>
</head>
<body>
    <h1>Happy Birthday, Surjo!</h1>
    <p>Wishing you a day filled with love, joy, and laughter!</p>
    <div class="quote">"Count your life by smiles, not tears. Count your age by friends, not years." – John Lennon</div>
</body>
</html>
"""

with open("surjo_birthday.html", "w", encoding="utf-8") as f:
    f.write(html_content)

print("HTML birthday file created!")